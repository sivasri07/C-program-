#include <stdio.h>
int main() {
    int n;
    scanf("%d", &n);
    if (n <= 1 || n >= 9) {
        printf("Invalid input");
    } else {
        for (int i = 1; i <= n; i++) {
            printf("%d x %d = %d \n", n, i, n * i);
        }
    }

    return 0;
}
